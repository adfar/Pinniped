[package]
name = "pinniped-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "pinniped_core"
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib", "lib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
